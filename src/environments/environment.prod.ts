export const environment = {
  production: true,
  /**
   * Base URL of the production backend API deployed on Render.
   * Replace the placeholder below with your actual Render service URL.
   * Example: 'https://your-service.onrender.com/api'
   */
  apiBaseUrl: 'https://jwt-mtn8.onrender.com/api',
}
