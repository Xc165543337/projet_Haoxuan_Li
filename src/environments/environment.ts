export const environment = {
  production: false,
  /**
   * Base URL of the remote backend API (set to Render deployment once available).
   * Example: 'https://your-service.onrender.com/api'
   * For local development, you might use: 'http://localhost:3000/api'
   */
  apiBaseUrl: 'http://localhost:3000/api',
}
